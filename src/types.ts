/**
 * A record of a text and its identifier.
 */
export interface SeedText {
    /**
     * The text possibly with with markup as a string.
     */
    text: string;

    /**
     * The text's identifier.
     */
    id: string;
}
